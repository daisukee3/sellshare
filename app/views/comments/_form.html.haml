= form_with model: [tweet, comment], url: tweet_comments_path(@tweet) do |f|
  %div
    = f.text_area :content, class: 'sm  form-control js-text'
    %p.js-text-count.pull-left
  .actions.pt-3
    = f.submit "コメントをする", class: 'btn-secondary'
