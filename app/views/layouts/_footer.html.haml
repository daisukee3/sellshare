%footer.footer-container.text-center
  %hr/
  - if I18n.locale == :en
    = link_to '日本語', url_for(locale: :ja)
  - else
    = link_to 'English', url_for(locale: :en)
  .sitemap__link
    = link_to 'お問い合わせ', 'mailto:nabanaba1225@gmail.com'
  .copylight
    %p Copyright ©︎ Daisuke All Rights Reserved.